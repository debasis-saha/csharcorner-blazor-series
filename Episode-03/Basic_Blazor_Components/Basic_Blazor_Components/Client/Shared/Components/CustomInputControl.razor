<input type="text" placeholder="@PlaceHolderCaption" @attributes = "AdditionalParameters"/>

@code {

    [Parameter] public string PlaceHolderCaption { get; set; }

    [Parameter(CaptureUnmatchedValues = true)] public IDictionary<string, object> AdditionalParameters { get; set; }
}
